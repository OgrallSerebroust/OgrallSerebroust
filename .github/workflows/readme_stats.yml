name: Readme-stats updater
on:
  workflow_dispatch:
jobs:
  generate_and_commit_cards:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - name: Create assets directory if it doesn't exist
        run: mkdir -p assets
      - name: Download stats cards
        run: |
          cards=(
            "api|stats_dark.svg|show_icons=true&theme=holi&show=prs_authored,prs_merged,prs_merged_percentage"
            "api|stats_light.svg|show_icons=true&theme=default&show=prs_authored,prs_merged,prs_merged_percentage"
            "api/top-langs|top_langs_dark.svg|layout=donut&theme=holi"
            "api/top-langs|top_langs_light.svg|layout=donut&theme=default"
          )
          for card in "${cards[@]}"; do
            IFS="|" read -r endpoint output params <<< "$card"
            curl --fail --location \
            "https://github-stats-extended-silk.vercel.app/${endpoint}?username=${{ github.repository_owner }}&${params}" \
            -o "assets/${output}"
          done
      - name: Commit downloaded cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/*.svg
          git commit -m "Update README cards" || exit 0
          git push